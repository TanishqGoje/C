#include <stdio.h>
#include <math.h>
#include <conio.h>

int main()
{

    /*
        Increment Operator:
        ++i (Pre increment operator)
        i++ (Post increment operator)

        Decrement Operator:
        --i (Pre decrement operator)
        i-- (Post decrement operator)
    */
    //  for(initialisation; condition; updation){
    //  do something
    //  }
    for (int i = 0; i <= 10; i++)
    {
        printf("%d\n", i);
    }

    return 0;
}
// here "i" can be called iteraor (or) counter