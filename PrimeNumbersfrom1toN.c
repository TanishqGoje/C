#include <stdio.h>

int main()
{
    int n, c = 0;
    printf("Enter till which number primes are to be printed : ");
    scanf("%d", &n);

    for (int i = 0; i <= n; i++)
    {
        for
    }
}